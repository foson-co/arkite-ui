---
---

Repo tooling only: add `.pre-commit-config.yaml` with detect-secrets plus a `.secrets.baseline`, and wire them into the existing Husky pre-commit hook. No consumer-facing change — nothing under `files` is touched.
